import 'package:flutter/cupertino.dart';

class MyEventCard extends StatelessWidget {
  const MyEventCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Text('Hello'),
    );
  }
}
